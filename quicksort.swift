import Foundation

extension Collection {
    var indexMiddle: Index {
        self.index(self.startIndex, offsetBy: count / 2)
    }
}

extension Array where Self.Element : Comparable {
    mutating func quicksorted() {
        var arrayCopy = self
        
        
        func quickSort() {
            var middleIndex =
            for currentIndex in arr {
                if currentIndex > middleIndex {
                    
                }
            }
        }
        
    }
}

